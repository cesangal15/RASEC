# BACKLOG — TM2 Sur

## V1 — Alcance actual (✅ cerrado jun-2026)

| # | Ítem | Estado |
|---|---|---|
| 1.1 | Login multi-rol (admin, encargado, 5 capataces, 2 chequeadoras) | ✅ Hecho |
| 1.2 | Formulario capataz: actividades con producción adaptativa + equipos anidados | ✅ Hecho |
| 1.3 | Formulario chequeadora: origen + viajes por PK destino | ✅ Hecho |
| 1.4 | Panel encargado: bandeja con toggles, totales, edición, Enviar a DATA, WhatsApp | ✅ Hecho |
| 1.5 | Backend Apps Script v6 (BANDEJA / MAQUINARIA / DATA) | ✅ Hecho |
| 1.6 | DATA alineada al maestro TM2 (paste A:S) | ✅ Hecho |
| 1.7 | ZODME automático; cereo no_data; estado derivado | ✅ Hecho |
| 1.8 | Hoja RESUMEN_MES en Modelo_Produccion (períodos 16–15, producción individual) | ✅ Hecho (validar con datos reales) |
| 1.9 | Máquinas nuevas en dim (FNG02, CR08–CR26, PEXC027) | ✅ Hecho (datos de referencia) |
| 1.10 | Prueba punta a punta del flujo bandeja → DATA con día real completo | ✅ Hecho (probado en ejecución real con chequeadoras enviando; residuales menores derivados a V2: WhatsApp de prueba = 2.6, ajustes de interfaz de DATA) |
| 1.11 | Alinear hoja MAQUINARIA del Sheets al orden de Captura_Diaria | ✅ Hecho (Codigo.gs: layout A→AA + internos tras AA, derivaciones H/I/R/O/T y flag a_captura, D52). Hoja MAQUINARIA recreada al layout D52 (respaldo MAQUINARIA_OLD) y redespliegue verificado con reporte real |
| 1.12 | Completar catálogo de máquinas (IDs reales de vibros/alquiladas faltantes) | ✅ Hecho |
| 1.13 | Corregir marca/modelo/valor-hora de máquinas nuevas en dim | ✅ Hecho |
| 1.14 | Contraseñas definitivas para encargado y chequeadoras | ✅ Cerrado — capataces y chequeadoras con clave definitiva; **el encargado se queda con su placeholder por decisión del dueño (jul-2026): no se va a cambiar, no volver a listarlo como pendiente** |
| 1.15 | Desglose por placa en reporte de chequeadora (textarea + parser, hoja VOLQUETAS) | ✅ Hecho (validado con datos reales, jun-2026) |
| 1.16 | Confirmación de envío en reporte-chequeadora.html (cuenta de filas del servidor, igual que capataz, D30). Detectado al validar 1.15. | ✅ Hecho |
| 1.17 | Detección de máquina duplicada por `reporta` en el panel del encargado (D51): solo es duplicado si el mismo `id_maquina` viene de dos capataces distintos | ✅ Hecho (encargado.html: agrupa por id_maquina; 1 capataz = reparto D46 con horas muertas = prog − Σ operadas; ≥2 capataces = conflicto con toggle ✓/✕ incluir/descartar) |
| 1.18 | Carga de minicargador (NH421, DINISSAN, alquilada 5h) y minibuldózer (CR026, ORTIZ, propia 6.4h) — ambos sin producción (igual a vibrocompactadores; IDs reasignados desde la lista de vibros descartada por D35). **+ limpieza de catálogo:** eliminar la pajarita PEXC027 del listado de máquinas en código; en reporte-chequeadora.html el selector de máquina muestra **solo excavadoras** (no el resto de tipos). | ✅ Cerrado — desplegado y validado en campo (jul-2026) |

## V2 — Mejoras identificadas (no iniciar sin cerrar V1)

| # | Ítem | Origen |
|---|---|---|
| 2.1 | **Emparejamiento chequeadora↔capataz anclado en la actividad del capataz, por ZONA de PK (no fila a fila).** Bucket terraplén = núcleo + corona del capataz (ambos mapean a TERRAPLEN) contra las filas OFICIAL de la chequeadora. Empareja por UF (filtro duro) + cercanía usando el **punto medio** del rango del capataz vs el PK de la chequeadora (los rangos vienen sucios —invertidos, con typos, p.ej. 38+500–35+800—; nunca usar los extremos). **Muchos-a-muchos por zona:** varias filas de chequeadora en una zona se **suman** (un PK recibe varios orígenes) y los frentes de capataz de esa zona comparten la suma; el cuadre se valida a nivel de zona. **Desfase sistemático** (3 días reales): el frente de albert sale ~35+xxx (capataz) vs ~34+6xx (chequeadora) → la tolerancia debe absorber ~500–800 m; parqueado, una mini-tabla de equivalencias de PK para frentes recurrentes (no construir aún). **Sin pareja clara / huérfanos** (volumen oficial sin frente = capataz que no reportó; o frente sin chequeadora) → el sistema no los empareja: los marca y el encargado los confirma a mano. **❌ CERRADO SIN IMPLEMENTAR — decisión del dueño (jul-2026): ya no importa.** El emparejamiento automático se abandona; la reconciliación se queda como está hoy: automática para el duplicado evidente (D51) y manual en el panel del encargado. Con esto 2.4 deja de depender de nada — su panel manual (D59/D60) es la solución definitiva, no un puente. | D29, refinado con 3 días reales jun-2026 |
| 2.2 | Clima en el reporte del encargado | ✅ Hecho (jul-2026, D37): dropdown en `encargado.html`; se sella en columna interna `clima` de DATA (no va al maestro), sale al WhatsApp y al resumen del jefe (`climaPorDia` en `?action=consolidado`). Pendiente validar en campo + redeploy. Drenajes sin clima por ahora. | D37 |
| 2.3 | **❌ CERRADO SIN IMPLEMENTAR — decisión del dueño (jul-2026): ya no importa.** Festivos marcados en RESUMEN_MES (rojo, como el archivo histórico). Era formato del Excel `Modelo_Produccion`, no tocaba la app. | D38 |
| 2.4 | **Reemplazar la producción estimada del capataz por el volumen reconciliado de la chequeadora**, solo para máquinas reportadas por el capataz cuyo frente sea **excavación o terraplén/ZODME** (02.05/02.06/02.07/02.08). Excluye subbase/base/BTC/MSR/desmonte (ahí el dueño del número es el capataz, ya es oficial) y vibros/apoyo (producción nula). Producción de la máquina = volumen oficial de **su zona** (la suma de 2.1); si hay 2+ máquinas en la zona, ÷ nº de máquinas (consistente con D54). **✅ Hecho como panel manual** (`produccion-maquinaria.html`, D59): admin consulta una fecha y ajusta la producción de MAQUINARIA con el oficial de DATA; cruce por **CC + proyecto/UF** (las filas de MAQUINARIA no llevan PK por el layout D52, así que el emparejamiento fino por zona de PK de 2.1 sigue pendiente para automatizarlo). Excavación prellena oficial÷nº máquinas (D54); terraplén/ZODME en blanco para reparto manual. Endpoint nuevo `?action=maquinaria_produccion` (GET cruce / POST parcha solo col T); guarda el estimado original en `produccion_capataz_orig`. **Resuelve la tensión de D55** por una vía aislada: el ajuste va directo a MAQUINARIA, **nunca** a DATA. **Ampliado (D60):** el panel muestra el panorama del día (frentes por actividad con su oficial aunque no tengan máquina, demás máquinas productivas en lectura, indicador multi-actividad) y permite **redirigir** la producción huérfana (ZODME, no aprovechable) a cualquier máquina creando su fila en MAQUINARIA. **D61:** muestra PK oficial (DATA) y PK del capataz por máquina + horas; lista las máquinas faltantes y permite registrar sus horas manualmente. **D62:** motivo de horas (estado derivado) + faltantes asignables a un frente o a una actividad complementaria sin producción (cereo/compactación/apoyo), saliendo de la lista al asignarlas. **✅ Cerrado — desplegado y validado en campo (jul-2026).** | Conversación maquinaria; refinado jun-2026; panel + panorama jun-2026 |
| 2.5 | **❌ CERRADO SIN IMPLEMENTAR — decisión del dueño (jul-2026): ya no importa.** Actividades secuenciales adicionales (iban a venir con las máquinas nuevas). Si algún día hace falta una actividad nueva, se agrega al catálogo (05_CATALOGO) como cualquier otra, sin necesidad de este ítem. | Usuario |
| 2.6 | Generación del mensaje WhatsApp con desglose idéntico al formato histórico (equipos por actividad, secciones MSR) | ✅ Hecho (jun-2026) — MSR sin total mixto + líneas por nombre de actividad; excavación aprovechable agrupada por banco de origen con subtotales (D56) |
| 2.7 | Registro de flota de volquetas (Ortiz vs particulares) | ✅ Resuelto fuera de la app — el cruce placa→empresa/cubicaje vive en la Bitácora de Transporte; la app solo lee cubicaje vía hoja CUBICAJE (D53) |
| 2.8 | Modo sin conexión Nivel 2 (captura offline + cola + sync) en capataz y chequeadora; encargado fuera. Requiere UUID en cliente para dedupe (toca Codigo.gs) y sesión en localStorage — **en implementación (D82)** | D49/D82 |
| 2.8b | Extensión del modo sin conexión a reporte-drenajes.html y asistencia.html (asistencia sin UUID: upsert idempotente verificado) | D82 |
| 2.9 | Escalamiento a Nivel 3 (service worker / PWA, carga sin señal desde cero) — **en implementación (D82)**: confirmado en campo que los celulares llegan al frente sin señal | D49/D82 |
| 2.10 | **Cubicaje real por placa** (primer ítem de V2): hoja CUBICAJE (`placa·cubicaje`), cálculo del volumen por línea en backend = Σ(viajes×cubicaje), fallback al factor del reporte (14) + flag (naranja en la chequeadora + columna `cubicaje_origen` en VOLQUETAS), columnas nuevas en VOLQUETAS (cubicaje, m3_placa, cubicaje_origen), endpoint GET `?action=cubicaje` y lectura en vivo en reporte-chequeadora.html. Empresa fuera (vive en la Bitácora). **Ajustes post-prueba (D54):** fallback 14 fijo (se quita el editor de m³/viaje), maquinaria de la chequeadora (excavadoras → MAQUINARIA, producción = total ÷ nº máquinas), tipo de destino ODT. | D53, D54 |
| 2.11 | **Desmonte y descapote en m²** (antes Ha): captura única en m² que dispara automático la fila contractual en Ha (÷10 000) + excavación no aprovechable (m²×espesor) que encadena ZODME (D17). Extiende D16/D17 y catálogo §1. **Resuelto por opción B** (genera Ha contractual + no aprovechable + ZODME, no "solo m³"): se conservan ambas salidas a DATA. El doble conteo con D08 **no aplica**: la chequeadora no maneja datos de desmonte/descapote, el capataz es la única fuente. Espesor editable (default 0.2). Ver D58. | ✅ Hecho (validar en campo) |
| 2.12 | **Arreglar la generación de la hoja DATA (D63).** Dos arreglos dentro de A–T (D23, sin columnas nuevas): **(1) ELEMENTO arreglado** — la hoja BASE **se mantiene** como catálogo de elementos. La BASE son **varias tablas lado a lado**; la de elementos (J/K/L) no se cruza por CC/descripción (otra tabla — ese era el bug del `"REVISAR ·"` casi siempre). El elemento se elige por **actividad** y se cruza por abscisa: préstamo→`EL DIVISO`, estructuras/MSR→`MSR …` del PK, conformación (02.08, incl. ZODME auto)→destino elegido por el residente (D79): `RCD 15+800`/3701 (default) o `ZODME PK30`/3702, resto→tramo `"tm2 pk X - Y"`. Los `ODT*`/drenajes se ignoran (fuera de alcance). REVISAR queda solo para PK fuera de tramo o marcador faltante. Respaldo (PK sin tramo) y frontends: helper único (`buildElemento`/`pkMeters`/`pkNorm`, igual en backend y HTML): `"tm2 pk NN+NNN"`/`"… - NN+NNN"`, normaliza a `NN+NNN` y no duplica el token `pk`/`Pk` (mata el `"Pk Pk"`). El backend deriva UF/PROYECTO/CC/ABS del PK (D04) para toda fila, incluida la vía que salía sin UF. **(2) Excavación de la chequeadora por ORIGEN, acumulada** — 1 sola fila de excavación por reporte = Σ líneas, al PK del origen (Masivo2 19+800 / Masivo1 14+400 / Diviso 21+500 / Compl-Otro = PK tecleado); el terraplén no cambia (1×línea al destino); ZODME auto intacto. Reportes del mismo origen quedan separados. **✅ Hecho (validar en campo + redeploy).** | ✅ Hecho (D63) |
| 2.14 | **Panel del jefe (`jefe.html`) — resumen post-DATA por rango + copiado A:S día a día (D65).** Rol nuevo `jefe`; pantalla de **solo lectura** que consulta DATA por rango de fechas (endpoint nuevo `?action=consolidado&desde=&hasta=`, en paralelo al de un día). **Resumen por actividad y ubicación** (subtotal de LARGO por unidad + filas por PK crudo inicial–final con badge UF, sin mezclar unidades) con filtros dinámicos de actividad/UF. **Copiado A:S día a día** al portapapeles (celdas de fórmula vacías para Omitir blancos, decimal con coma). Acceso para jefe/admin/residente (cierra el "Resumen General" placeholder de D57). Navegación con tile en `menu.html` y botón "← Menú" para admin en todas las pantallas. **No escribe DATA/BANDEJA/MAQUINARIA ni el maestro.** | ✅ Hecho (validar en campo + redeploy) | Pedido del usuario + feedback del jefe (jul-2026) |
| 2.15 | **GRUPO de estructuras/MSR = TIERRAS (D66).** Las líneas 05.* salían a DATA con GRUPO "DRENAJES Y ESTRUCTURAS" cuando la BASE las tiene en TIERRAS (el CAPÍTULO ESTRUCTURAS es correcto). Corregido en `reporte-capataz.html`/`encargado.html` + red de seguridad en `buildDataRow`. | ✅ Hecho (redeploy) | Reportado por el usuario (jul-2026) |
| 2.16 | **Chequeadora: Botadero = no aprovechable + destinos visibles (D67).** El material con destino Botadero genera su propia fila acumulada de excavación NO APROVECHABLE al PK del origen (dispara ZODME D17); el resto de destinos sigue en la fila aprovechable/préstamo del origen. La observación de las filas de excavación sella el desglose "→ Terraplén 300 · Puente 150 m³", visible en el panel del encargado y el WhatsApp (explica excavado vs terraplén). | ✅ Hecho (validar en campo + redeploy) | Reportado por el usuario (jul-2026) |
| 2.17 | **DATA: ABS desde el elemento/subtramo + DESCRIPCION/ELEMENTO verbatim de la BASE (D68).** (a) ABS INICIAL/FINAL de filas con match toman K/L del elemento de la BASE verbatim (pedido del jefe), no el PK reportado; sin match, del PK. (b) DESCRIPCION verbatim de la BASE (cruce por CC, tabla de ítems A–H) en vez del catálogo interno; ELEMENTO en filas con match ya salía verbatim de la celda J desde D63 (verificado en inspección; `buildElemento` solo para el fallback). PK reportado se conserva en internas U–AA. Si un elemento con match sigue sin emparejar en el maestro, el carácter raro vive en la celda de la BASE → limpieza única sobre la BASE. | ✅ Hecho (validar en campo + redeploy) | Pedido del jefe + bug de fidelidad (jul-2026) |
| 2.18 | **Expansión a DRENAJES: áreas ODT/ODL (D70).** `deriveArea(cc)` (06.*→ODT, 07.*→ODL, resto tierras, sin columna nueva en DATA); `enviar_data` pisa por **día + área** (enmienda operativa de D03); BANDEJA 23→28 cols (`area` + personal/turno_noche/nota_libre SOLO-WhatsApp); MAQUINARIA +interno `area` (máquinas de drenajes en captura libre con `a_captura=NO`); rama drenajes en `buildDataRow` (ODT: ELEMENTO=marcador `ODT*` con ABS K/L verbatim; ODL: tramo `"tm2 pk X - Y"` o marcador por descole; GRUPO "DRENAJES Y ESTRUCTURAS", red D66 acotada a tierras); endpoint `?action=drenajes` (147 marcadores + ítems .06/.07 verbatim de la BASE); `?action=bandeja&area=`; pantallas nuevas `reporte-drenajes.html` y `residente-drenajes.html`; usuarios `capataz_odt/odl` y `residente_odt/odl`; filtro Área + copiado por área en `jefe.html`. | ✅ Cerrado — desplegado y validado en campo (jul-2026) — D70 |
| 2.19 | **Conversión de unidades en drenajes (ml→m³, und→m).** Parqueado: se define después con los capataces; V1 captura cantidades directas en la unidad contractual. | ⏸️ Parqueado (V2) — D70 |
| 2.21 | **Login validado en el backend (claves fuera del archivo público).** Hoy las contraseñas viven en `index.html` (GitHub Pages, código fuente visible para cualquiera). Mover la validación al Apps Script contra una hoja del Sheet (p. ej. USUARIOS: `usuario·clave·rol·redirige`) para que las claves no estén en el archivo público y se gestionen editando la hoja. El usuario decidió (jul-2026) dejarlo como está por ahora y revisar esta opción más adelante. | ✅ **CERRADO por D108 (jul-2026)** — claves fuera de `index.html`, a la hoja privada `USUARIOS` (con hash SHA-256 vía `endurecerClaves()`), validadas por `POST {action:'login'}`. El modo sin conexión se conserva con sesión recordada + credencial recordada: señal UNA vez por teléfono. **No es autenticación de extremo a extremo** (los endpoints siguen abiertos y el rol vive en el navegador); eso exigiría token por petición y choca con la cola offline |
| 2.20 | **Catálogo real de máquinas de drenajes y su paso a Captura_Diaria.** Parqueado: hoy captura libre (texto) con `a_captura=NO`; cuando exista catálogo se definirán tipos/horas programadas y el par H/I. | ⏸️ Parqueado (V2) — D70 |
| 2.22 | **Pantalla de digitación de volquetas (`digitadora.html` + endpoint `?action=volquetas`).** La digitadora transcribe 300–500 partes físicos/día en la BASE de transporte (`TERRAPLEN.xlsx`, hoja `BASE 2026`). La pantalla nueva le entrega los renglones ya pre-llenados desde la hoja **VOLQUETAS** (placa, PKs, cubicaje, UF, actividad/material/CC por `tipo_destino`) para que solo teclee remisión, horas y conductor y corrija lo que difiera. **Explosión a nivel viaje** (default): Σ `viajes` = renglones, cada uno con su **PK destino real editable** (programado = guía; resalta naranja al diferir); **toggle agrupado** por grupo (ORTIZ/internos = 1 fila con `Cant. viajes`=N, sin horas); **viajes externos** a mano; **sin persistencia** (Opción A: la app solo pre-llena y exporta, la remisión/empresa siguen fuera — D53/2.7). Export alineado A→AH con **Omitir blancos** (H/M/V/W/AB–AH son fórmula, nunca se escriben): "Copiar todo" (TSV) y "Descargar CSV" (UTF-8+BOM, `;`). Mapeo `tipo_destino`→Actividad/Material/CC (Terraplén CC `37xx.02.11` por PK destino; Puente CC fijo `3701.11.03`; ODL/ODT/Botadero con CC en blanco; Botadero además sin Actividad/Material); Masivo 2 = PK 19800. Endpoint GET **solo lectura** de VOLQUETAS. Usuario `digitadora`/`volquetas2026` (rol `digitadora`, entra directo; admin vía `menu.html` + "← Menú"). Offline fuera de alcance (D49/D82). | ✅ Cerrado — desplegado y validado en campo (jul-2026) — D83 |
| 2.25 | **✅ HECHO por D109 (jul-2026) — Autenticación por TOKEN FIRMADO.** Ítem nuevo, abierto y cerrado en la misma sesión tras comprobar sobre el propio código que D108 (claves fuera del archivo público) **no cerraba el agujero de fondo**: el rol vivía en el navegador y el backend se creía la identidad que le mandaba el cliente (`e.parameter.usuario`, `body.usuario`), así que bastaba `localStorage.setItem('rol','admin')` o una llamada desde terminal diciendo «soy la residente» —sin contraseña— para leer y ESCRIBIR. Al entrar, el backend emite ahora un token con `usuario·rol·áreas` firmado con HMAC-SHA256 y un secreto guardado en las Propiedades del Script; `doGet`/`doPost` de los DOS backends lo verifican en una puerta única y **sobrescriben la identidad** con la del token, así que el resto del código sigue igual pero ya autenticado. No caduca por reloj sino por versión (`AUTH_V` para sacar a todos; `estado` en `USUARIOS` para sacar a uno), que es lo que lo hace compatible con la cola sin señal. En el cliente, `auth.js` envuelve `fetch` y adjunta el token a toda llamada a la API — así ninguna de las ~60 llamadas de las 13 pantallas se puede quedar sin él por descuido, y la cola lo pega **al enviar**, no al encolar. `AUTH_ESTRICTO` deja volver a modo tolerante en una edición. **Ojo al desplegar:** las Propiedades del Script son por proyecto, así que el secreto se copia a mano de obra (emisor, `mostrarSecretoAuth()`) a asistencias (verificador, `fijarSecretoAuth("…")`); si falta, asistencias lo dice con la causa exacta en vez de rechazar a todo el mundo sin explicación. | D108, análisis jul-2026 |
| 2.13 | **Informe mensual de maquinaria alineado al reporte del jefe.** El jefe pasa un reporte externo con las actividades de las máquinas; el informe mensual debe espejarlo **sin tocar Captura_Diaria**. **V1 (D64, junio):** tabla `fact_jefe` (gemela de `fact_produccion`) con las máquinas/actividades/horas del jefe y la producción real de Captura **redistribuida** día a día (proporcional a horas; invitadas en 0; cruce UF; salto al día anterior), generada con la herramienta `Generar_fact_jefe.html`. **V2 — vigente (D86, julio): reparto MENSUAL de un solo paso, ya no parte de la producción real por máquina.** Herramienta nueva **`Reparto_Produccion_Maquinaria.html`** (un solo .html, SheetJS, sin backend, patrón Bitácora): se cargan el **reporte diario de obra** (hoja `dia suelto` → producción por actividad/día, UF1→3701 · UF2→3702) y los **partes del jefe** (hoja del mes → horas por máquina y CC, con el **corte** leído de `FECHAS DE CORTE`), y reparte por **coherencia de tipo** (02.05/02.06→EXC · 02.07→BULL 80% + MOTO 20% · 02.08→BULL · 03.01/03.03→MOTO · 05.04→BULL+MOTO), **proporcional a horas** con cascada CC exacto → ítem en cualquier UF → horas totales (nunca queda producción huérfana), con la **no aprovechable acreditada dos veces** (02.05 excavación + 02.08 ZODME) y switch para cruzar UF en el ZODME. **Salida = la tabla `fact_jefe` (D89):** los informes `Informe_Mensual` y «Maquinas Junio» son fórmulas `SUMIFS` sobre esa tabla filtrando por **día**, así que el .xlsx trae la hoja `fact_jefe` con las **29 columnas A:AC** del maestro y **una fila por máquina · día · actividad** (más `reparto` y `cuadre` como auxiliares). El reparto se hace **por día** con la producción diaria de `dia suelto`; el vocabulario de `actividad`/`SUB ACTIVIDAD` es el exacto que filtran las fórmulas (núcleo de terraplén al bulldozer, corona a la motoniveladora). Lee los **dos formatos** de parte (día a día y bloques mensuales), toma el tipo de máquina del **catálogo de la flota** (el parte llega a decir «RETROCARGADORA» de una excavadora), filtra a **3701/3702** (el parte trae todos los frentes de la empresa) y avisa **qué máquinas hay que agregar a mano** a los bloques de `Informe_Mensual`. No escribe los maestros ni toca el app web. Office Scripts bloqueado por la cuenta; VBA evitado por archivo compartido. Ver D64 y D86. **✅ CERRADO (jul-2026, decisión del dueño): sí es código y ya está entregado** — la herramienta `Reparto_Produccion_Maquinaria.html` vive en el repo y funciona; se cierra sin esperar más validación. **Estancias (D143, ago-2026):** la lista de máquinas lleva fechas `ingreso..retiro` (ventana semiabierta, una línea por estancia, como la hoja `MAQUINAS` de D138), y una máquina fuera de su estancia —o que no reportó horas ese día— **no recibe producción**; sus filas del parte se descartan enteras. Cierra que una máquina entregada a mitad del corte se llevara producción de días en que ya no estaba (3.468 m³ medidos en un caso real). **Días no digitados (D147, ago-2026):** si el rango dice que la máquina estaba y el parte no la menciona ese día, se agrega la fila en cero (sin horas ni producción, sin tocar el reparto), para que no falte en el informe. Con D143/D146: las fechas mandan sobre el parte en las dos direcciones. **Listón por actividad (D144, ago-2026):** con parte día a día, para recibir producción de una actividad hay que haberla hecho ese día —horas en ESE ítem, no el total del día— y con al menos las horas mínimas (parámetro, 1 h por defecto); un tipo entero que no la hizo cede su cuota del 80/20 al que sí. Cierra las filas de 0,2 h con 526 m³, la producción sin horas de un bulldozer parado, y que un bulldozer cargado solo a ZODME cobrara terraplén. **Clima (D140, ago-2026):** la columna **S (CLIMA)** de `fact_jefe` deja de salir en blanco — se llena con la columna `TIEMPO` de la hoja **`DATOS`** del reporte diario de obra (mismo archivo que la producción), igual para todas las máquinas de ese día y normalizada a mayúsculas. La **producción sigue leyéndose de `dia suelto`**: `DATOS` no es su espejo (no tiene MSR ni préstamo y sus totales difieren), pendiente de aclarar con el dueño. | ✅ Hecho (V2 entregada; pendiente validación con los archivos reales del mes) | Cambio de panorama del jefe (jun-2026 → jul-2026) |
| 2.23 | **Notas del día completas en los dos reportes (D103).** Cada reporte tenía la mitad: drenajes solo nota **por actividad** (`nota_libre`), tierras solo nota **general del día** (`observacion_general` → hoja OBSERVACIONES). Se añade la nota general a `reporte-drenajes.html` (payload `observacion_general` + `id_reporte`; el backend ya la guardaba y la sella por área con `areasDeReporte`, D86 ⇒ **sin cambios en `Codigo.gs`, sin redespliegue**) y se muestra en el panel de `residente-drenajes.html`, no solo en el WhatsApp. En tierras, la nota que el capataz escribe por actividad (col `observacion`, que ya viajaba a DATA col S) sale ahora como `📝` bajo su actividad en el WhatsApp y en el panel del encargado, filtrando con `notaManual` los sellos automáticos que no son notas (observación de la chequeadora D67/D80, filas ZODME `Auto · …`, filas `orig:descapote/desmonte` que copian la nota de su madre, y el tramo `→ destinos`). | ✅ Cerrado — desplegado y validado en campo (jul-2026) — D103 |
| 2.24 | **Subtramos en tierras: cruce por abscisa (frontera + ancla por punto medio) + corrección manual del residente.** Las líneas de tierras caían en subtramos incorrectos por dos causas de código: el intervalo se comparaba **cerrado en ambos extremos** (un PK en la frontera empataba con dos subtramos y desempataba el orden físico de las filas de la BASE) y el cruce usaba **solo el PK inicial** (`pk_final` llegaba pero no participaba del emparejamiento). ✅ **Hecho (D104):** intervalo **semiabierto `[ini, fin)`** con el último tramo del eje cerrado; **ancla = punto medio** del rango (tolera invertidos); ambos **gateados al conjunto TRAMO** para no romper los marcadores puntuales MSR, que compartían la misma rama de código. Override manual por línea (`elemento_forzado`, sin columnas nuevas en BANDEJA), endpoint `?action=tramos`, selector + resolución visible en `encargado.html` y ELEMENTO en las ubicaciones de `jefe.html` (solo lectura). Verificado en banco de pruebas. **Pendiente: validación en campo con reportes reales + redeploy del Apps Script** — y confirmar el umbral del aviso de borde (`SUBTRAMO_BORDE_M`, hoy 50 m). Relacionado con 2.1 (mismo criterio de nunca anclar en un extremo del rango), que **sigue pospuesto**. | Prompt del usuario (jul-2026); D104 |
| 2.26 | **Claridad del envío a DATA en el panel del residente/encargado (D110).** Reportado en obra: el residente revisa la bandeja, pulsa "Enviar a DATA" y cierra de una vez; el único acuse era un "enviado ✓" que se apagaba solo a los 3 s, así que no quedaba forma de saber si el día llegó al maestro — ni de notar las filas que el capataz/chequeadora mandaron DESPUÉS del envío. El reporte del capataz (pantalla de éxito, D82) y la asistencia (`confirmBox` permanente) ya tenían confirmación explícita; este panel no. **Hecho (jul-2026, solo frontend, sin redespliegue):** estado del envío permanente (vacío · sin enviar · enviado · cambios sin enviar) reconstruido desde el `estado` de BANDEJA, confirmación con resumen de lo que se envía, guardas contra el envío que BORRA el día, candado anti doble envío y aviso del navegador al cerrar con algo sin enviar. | Usuario (jul-2026) |
| 2.27 | **Terraplén distinguido por MATERIAL: `Terraplén con crudo de río` y `Terraplén de UF3` (D113).** Pedido del dueño (ago-2026): el capataz de tierras necesita separar con qué material se hizo el terraplén — crudo de río (material comprado) y material que envía UF3. **Dos actividades del catálogo, no un campo "material"**: la clasificación no se cruza con núcleo/corona (*"para estos dos no interesa, simplemente es terraplén"*), así que son dos entradas más en la lista y **no se toca el esquema de ninguna hoja**. A DATA salen estandarizadas (ítem `Terraplenes (solo conformación)`, CC `37xx.02.07`, GRUPO TIERRAS, CAPÍTULO EXPLANACIONES, DESCRIPCION verbatim de la BASE, copiado A:S igual); la distinción vive en la columna interna `actividad`. **Hecho (ago-2026):** las dos actividades en `reporte-capataz.html` y en la lista de "agregar línea" de `encargado.html`; par **TERRAPLEN / NUCLEO DE TERRAPLEN** en `CAPTURA_ACT_MAP` (sin él, sus máquinas dejaban de pasar a Captura_Diaria en silencio); **fuera de la reconciliación automática y de la validación "terraplén ≤ aprovechable + préstamo"** —la chequeadora solo mide los viajes desde los bancos de corte propio, así que apagarlas perdería volumen real y sumarlas dispararía la alerta roja sin error—, con la explicación junto al aviso; etiqueta de material en el WhatsApp; y en el panel del jefe, `?action=consolidado` expone la columna interna `actividad` y cada bloque de actividad muestra debajo de su total una fila **Desglose** por material (D113d: la línea no se saca de su actividad y el total no cambia; solo aparece cuando hay más de un componente, así que lo anterior al cambio se ve igual). **Pendiente: validación en campo + redespliegue del Apps Script** (`CAPTURA_ACT_MAP` y `consolidadoRango`). `CACHE_V` de `sw.js` no sube (la lista de precache no cambia). **Ampliado a DRENAJES (D113c):** el dueño pidió la misma distinción para las ODT, sobre el ítem `37xx.06.02` "Rellenos con material seleccionado" → `Relleno con crudo de río` y `Relleno de UF3`. Ahí SÍ fue código porque en drenajes la actividad ERA la descripción del ítem (catálogo vivo desde la BASE): se resuelve con **variantes de actividad** (`VARIANTES_DREN` en `Codigo.gs`) servidas en `?action=drenajes` como opciones del mismo ítem —mismo CC, misma DESCRIPCION verbatim, misma unidad—, así que la fila de DATA es idéntica en A–T y la distinción viaja otra vez por la columna interna `actividad`; el desglose por material del jefe las muestra dentro del bloque del ítem, sin tocar nada. El panel de drenajes pasa a agrupar por `actividad||descripcion` (para el histórico es el mismo string). **Piedra filtro (CC `3701.06.03`) queda parqueada** por costo/beneficio: obliga a abrir el mecanismo de áreas (ver D113). | ✅ Cerrado en código; pendiente campo + redeploy — D113 / D113c |
| 2.28 | **Catálogo de máquinas VIVO en la hoja `MAQUINAS` (D138).** Las máquinas entran y salen de la obra continuamente según la necesidad —alquilar y tener parado es caro, así que el finisher **FNG02** y el vibro **CR08** trabajan en pareja y solo se traen cuando hay BTC—, y hasta ahora cada alta o baja era editar `Codigo.gs` + cuatro pantallas y redesplegar. Era el ÚLTIMO catálogo del sistema escrito en código. Hoja nueva `MAQUINAS` (`id_maquina · tipo · horas_prog · propiedad · fecha_ingreso · fecha_retiro · notas`) con **una fila por ESTANCIA** y ventana **semiabierta** `[ingreso, retiro)`; endpoint `?action=maquinas&fecha=`; cliente compartido `flota.js` (servidor → caché del teléfono → respaldo de la pantalla, para no romper la captura sin señal de D82); respaldo del backend a `MAQ_CATALOGO` para no servir nunca una flota vacía. La recepción de reportes NO valida contra el catálogo, a propósito (cola offline). Semilla `backend/seeds/MAQUINAS.tsv` con el histórico 2026 reconstruido. | ✅ Hecho (pendiente validación en campo + **redespliegue**) | Pedido del dueño ago-2026, a raíz de D136/D137 |
| 2.29 | **Pestaña de FLOTA en la pantalla de maquinaria + apertura de accesos.** D138 mudó el catálogo de máquinas a la hoja `MAQUINAS`, y al cerrarlo quedó señalado el riesgo que este ítem ataca: el `id_maquina` lo teclea ahora una persona y **tiene que coincidir letra por letra con `dim_maquinaria`** o el pegado a `Captura_Diaria` deja de cruzar en silencio (D111); una hoja suelta no puede avisar, una pantalla sí. `produccion-maquinaria.html` pasa a llamarse **"Maquinaria"** con dos pestañas — **Producción del día** (lo de D59/D60/D61/D62, intacto) y **Flota** (altas, bajas y reingresos sobre la hoja, con estancias y ventana semiabierta) —; **guard de typos** comparando el ID contra los `id_maquina` del histórico de MAQUINARIA NORMALIZADOS (quitando lo no alfanumérico, `RT02`→`RT-02`), leyendo **solo esa columna** con el lector acotado de D107; endpoint nuevo POST `flota_guardar` con rol verificado **en el servidor** (D109), `fdateValida_` (D106) e invalidación de las DOS memorias (`invalidarHoja_` **y** `_flotaRows`, que tiene memo propio). **Accesos decididos:** `admin` y `residente` editan todo, **`jeisson` también edita la flota** (su cuadrilla es OPERADORES) y **`jefe` entra en solo lectura**, con su puerta en `jefe.html`. `jeisson` es el primer usuario de asistencias que entra a una pantalla de obra: aceptado a propósito, sin tocar el aislamiento de D69. **✅ Hecho (D139, ago-2026):** la pantalla se llama **Maquinaria** y tiene las dos pestañas; la Flota muestra las estancias agrupadas por máquina con el historial plegado y **los `avisos` de la hoja, que hasta ahora nadie veía**; alta, reingreso (fila nueva), baja y corrección; endpoint de lectura `?action=flota` y de escritura `POST flota_guardar` con el guard de rol en el servidor, `fdateValida_`, rechazo de clave duplicada y de traslapes, y las dos memorias invalidadas; el detector de parecidos compara contra los `id_maquina` del histórico de MAQUINARIA **leyendo solo esa columna** (340 celdas en vez de 12.040 con 300 filas). **Presentación corregida con el dueño tras la primera versión:** tres bloques en vez de una lista plana — **En obra hoy** arriba (una tabla, separadores por tipo), **Por llegar** si aplica y **Ya no están en la obra** PLEGADO —, con resumen (*en obra hoy / fuera* + chips por tipo) y buscador; contenedor de **escritorio** (1180 px, como `digitadora.html`/D83) solo en esta pestaña. Accesos abiertos a `residente`, `jeisson` (solo la flota) y `jefe` (solo lectura, desde `jefe.html`). Arnés hermano `backend/pruebas/verificar_d139_flota_pantalla.js` (111 comprobaciones, con 4 mutaciones deliberadas que lo prueban no-ciego). **Requiere redespliegue del Apps Script de obra.** Prompt de origen en `docs/PROMPT_2.29_pantalla_maquinaria.md`. | ✅ Hecho (D139) — pendiente validación en campo + redespliegue | Pedido del dueño ago-2026, a raíz del riesgo que dejó abierto D138 |

## Módulo Asistencias (en curso)

| # | Ítem | Estado |
|---|---|---|
| 4.1 | **Sistema de asistencia de personal de tierra** (antes 3.1) — módulo nuevo, aislado (Sheet + Apps Script propios, mismo repo/login, D69). Digitaliza las planillas físicas: `asistencia.html` (formulario del responsable, roster por cuadrilla + bloques por CC + ausencias), `resumen-asistencia.html` (residente/jeisson: resumen del día, faltantes, descarga Excel Navision, gestión de personal), `seleccion-reporte.html` (tiles para usuarios con doble deber). Backend `backend/CodigoAsistencias.gs` (`SHEET_ID` propio) con hojas PERSONAL/CUADRILLAS/ASISTENCIA/CONFIG/FESTIVOS/CAT_TRABAJADORES/CAT_CC/CAT_MOTIVOS y `setupHojas()` de un solo uso. | ✅ Cerrado — en uso diario y validado en campo (jul-2026) |
| 4.2 | **Parámetro abierto — mapeo columnas H–N (Dom/Fest c/s compensación).** Alcance reducido desde D72e/D77: el generador Navision YA llena **C ordinarias · D ord. Dom/Fest (tope 7h, criterio D72/D73) · E extra diurna · F extra nocturna · G recargo nocturno ordinario**, con el TURNO reportado como estándar (catálogo TURNOS; extras tras la salida del turno; ventana nocturna hasta `CONFIG.nocturno_hasta`=06:00). Solo queda abierto el mapeo fino **H–N** (Dom/Fest con/sin compensación); **Cerrado jul-2026 por confirmación del dueño:** no hace falta mapeo fino de I–N — esas columnas se quedan **vacías** en el Parte, que es exactamente lo que el generador ya hace. El punto de extensión `mapearColumnasEspeciales()` (con su TODO) se deja en `resumen-asistencia.html` por si algún día Navision rechaza un Parte por eso; si pasa, se reabre. | ✅ Cerrado — confirmado por el dueño (jul-2026): el comportamiento actual es el correcto, I–N se quedan vacías |
| 4.3 | **Parámetros abiertos — strings de proyecto en la hoja CONFIG (`proyecto_3702` y `proyecto_3703`).** Aclaración jul-2026, porque se confundían dos cosas distintas: **la PLANTILLA `.xlsx` sí es la misma** (el archivo es un contenedor: hoja `Parte` + catálogos `Proyectos`/`Centros de coste`/`Trabajadores`/`Motivos ausencia`); lo que cambia es **un string por proyecto** que el generador escribe en la **columna O «No. Proyecto»** y que Navision lee **verbatim**. No se deriva ni se inventa: `generarExcel` lo busca por clave `proyecto_<prefijo del CC>` en CONFIG y, si falta o dice `PENDIENTE`, avisa en naranja y escribe el número pelado (`3702`), que Navision **no** reconoce. Valores: `proyecto_3701` = `3701| T2 - UF1 - R4513 PR 09+800 - PR 30+000` (ya cargado; verbatim de la hoja `Proyectos` de `plantilla_parte.xlsx`); **`proyecto_3703` = `3703| T2 - UF3 - R4513 PR 09+800 - PR 90+718`** (verbatim de la hoja `Proyectos` de `plantilla_parte_uf3.xlsx` que entregó el dueño — **hay que pegar esa fila A MANO en el CONFIG vivo**; `setupHojas()` solo siembra con la hoja vacía); **`proyecto_3702` = PENDIENTE** — no está en ninguna de las dos plantillas del repo (la de 3701 trae SOLO la fila de 3701), así que hay que copiarlo de Navision o de una plantilla/exportación que cubra UF2. Nada que ver con `CAT_CC` / «Centros de coste», que es otro catálogo y ya está cargado. | ✅ Cerrado — los tres strings de proyecto (3701/3702/3703) ya están cargados en la hoja CONFIG viva (jul-2026, confirmado por el dueño) |
| 4.4 | **Refinamientos tras prueba con datos reales (jul-2026).** (a) **Super avisos en el formulario:** al enviar, confirmación fuerte y explícita (con la lista de nombres) si hay personas PRESENTES sin actividad/CC, o si alguien supera el turno + máximo de extras del día (días normales); se puede forzar pero con confirmación consciente (no bloqueo duro). (b) **Aviso de extras en el export:** el generador Navision marca en naranja las personas que exceden `max_extras_dia` para revisión manual (el mapeo fino a columnas de recargo sigue siendo el parámetro abierto 4.2). (c) **Plantilla recordada:** la `Plantilla_Parte_Trabajo…xlsx` se guarda en `localStorage` y se restaura sola — se sube UNA vez, no cada sesión (si no cabe en localStorage, cae a solo-esta-sesión). (d) **Completar faltantes desde el resumen:** residente/admin/jeisson pueden agregar/corregir en `resumen-asistencia.html` a las personas sin reportar (o presentes sin CC) con una mini-interfaz rápida (presente+CC+horas / ausente+motivo); endpoint nuevo `asistencia_individual` que hace **upsert por persona** sin pisar lo que ya reportó el responsable de la cuadrilla. | ✅ Cerrado — desplegado y validado en campo (jul-2026) |
| 4.5 | **Extender el módulo de Asistencias (4.1) a las cuadrillas de drenajes (ODT/ODL, D70).** Hoy `reporte-drenajes.html`/`residente-drenajes.html` solo registran oficiales/ayudantes/turno de noche como texto informativo para el WhatsApp (D70), no en el módulo de asistencia real. Pendiente: sumar los CC `06.*`/`07.*` a CUADRILLAS/CAT_CC cuando se decida incorporarlas al mismo flujo. | ✅ Cerrado — YA está hecho: las cuadrillas ODT/ODL entraron al módulo por D72 (data-driven vía la hoja CUADRILLAS) y D84 (residente unificado). El ítem se había quedado sin actualizar (jul-2026) |
| 4.6 | **Canal de extras del admin (`mis-extras.html` + `EXTRAS_ADMIN`), D73.** El admin registra sus horas extras de días puntuales (máx 2h/día) en una hoja aparte `EXTRAS_ADMIN` (aislada del roster); el generador Navision inyecta su fila al `Parte` del día×proyecto (E diurna / F nocturna; Dom/Fest → col D ordinarias dom/fest, tope 7h — aclaración del dueño en D73). Endpoints `extras_admin` (GET/POST) y `extras_admin_delete`; tile admin en `menu.html`; indicador en el resumen. Parámetro abierto `admin_recurso` (No. Recurso de Navision) en CONFIG y flag `EXTRAS_ORDINARIAS_EN_CERO`. | ✅ Cerrado — `admin_recurso` ya cargado en la hoja CONFIG viva (jul-2026) |
| 4.7 | **Reorganización de áreas: salida a UF3 + drenajes unificados (D84).** Salida a UF3 de `ariel`/`albert`/residente ODT (logins eliminados, histórico intacto); columna `estado` en CUADRILLAS (inactivar sin borrar; filtra roster/faltantes/export/selectores/gestión, no lo ya reportado); residente unificado `residente_dren` (rol → `['odt','odl']`) con bandeja combinada (una llamada `enviar_data` por área + guard anti-borrado, WhatsApp con toggle) y **export Navision combinado ODT+ODL** en un archivo; capataz de drenajes con acceso a ambos capítulos (campo `areas`, área por CC, no-regresión para una sola área); `areaDeUsuario()`→`areasDeUsuario()` (array, `includes`); mover personal entre cuadrillas ODT↔ODL. Backend de obra sin cambios; se sube `CACHE_V`. | ✅ Cerrado — desplegado y validado en campo (jul-2026) — D84 |
| 4.8 | **Archivado / rotación del histórico de BANDEJA, DATA, MAQUINARIA y VOLQUETAS por año o período.** Las hojas de escritura crecen sin tope: D93 hace que la grilla se amplíe sola cuando se agotan las filas (`ensureRows_`), lo que resuelve el fallo inmediato, pero el archivo completo tiene un techo de **10 millones de celdas** sumando todas las hojas. Cuando el histórico pese, habría que mover lo cerrado (p. ej. años anteriores) a un Sheet de archivo y dejar en el vivo solo el período en curso, sin romper los cruces que hoy leen todo el histórico (`acumulado_drenajes`, `proyectoDefecto` del export de asistencias). **No se implementa en D93** (ese cambio es solo capacidad de grilla). Un aviso automático al acercarse al límite de celdas entra en el mismo ítem. **Actualización jul-2026 (D107): sigue abierto pero ya sin urgencia.** Lo que apretaba no era el techo de celdas del archivo sino el coste POR PETICIÓN, y eso lo resolvieron D102 (lecturas de asistencias) y D107 (escrituras de asistencias + lecturas de obra). Este ítem se retoma cuando el archivo se acerque de verdad a los 10 millones de celdas, no antes. | D93 |
| 4.9 | **Seguimiento de ausencias por rango (D94).** Sección plegable nueva en `resumen-asistencia.html` + endpoint GET `?action=ausencias&desde=&hasta=` (solo lectura, tope 186 días, acotado al área del usuario). Responde "de tal fecha a tal fecha, quién faltó y por qué": ausencias reportadas con su motivo + (opcional, apagado por defecto) días en que la cuadrilla reportó pero a la persona no la incluyeron; filtros por motivo/cuadrilla/nombre, desglose por motivo, vistas por persona (reincidencias, con fechas) y por fecha, y descarga CSV de lo filtrado. No escribe nada. | ✅ Cerrado — desplegado y validado en campo (jul-2026) — D94 |
| 4.10 | **Rendimiento de `resumen-asistencia.html` — Fase 1 (frontend) hecha, Fase 2 (backend) pendiente de visto bueno (D99).** Diagnóstico completo en `docs/INFORME_PERF_resumen-asistencia.md`. **Hecho (sin redeploy):** render progresivo (KPIs y cuadrillas primero; detalle de D76, "sin reportar" y "completar faltantes" se arman al desplegarlos), SheetJS y `plantilla_parte.xlsx` bajo demanda con precarga en segundo plano, `Promise.all` en el export, indicador de carga no bloqueante (la pantalla ya no se borra al cambiar de fecha) e instrumentación `DEBUG_PERF` (`console.table` con ms/KB por petición). Medido: **12.166 → 222 nodos DOM** y **204 → 1 ms** de pintado con 225 personas; export idéntico celda a celda. **Hecho también — Fase 2, puntos 1–4 (autorizados tras reportar esperas de 10–30 s; EXIGE REDEPLOY):** apertura única del Spreadsheet por ejecución (`ss_()`) + memoria de lectura dentro de la misma petición (`_memoHoja`, con invalidación en los 10 puntos de escritura) → **`openById` 14 → 1** y cero relecturas de hoja; `getRange(1,1,lastRow,nCols)` en vez de `getDataRange()`; `?action=export` deja de leer `ASISTENCIA` dos veces (**178.524 → 89.563 celdas**); y campo **`_ms`** en toda respuesta, que el frontend ya separa en `servidor_ms`/`red_ms`. Verificado con un arnés que corre el `.gs` en Node: **21 respuestas idénticas** carácter a carácter salvo el `_ms`. **Hecho también — punto 5, ya con la medición en la mano** (`servidor_ms` 5.200–5.456 de ~7.000 totales; pintado del cliente 2 ms): `CacheService` 6 h para las 10 hojas casi estáticas (CONFIG/FESTIVOS/TURNOS/CAT_CC/CAT_TRABAJADORES/CAT_MOTIVOS/MOTIVOS_USADOS/CC_USADOS/CUADRILLAS/PERSONAL; **nunca** ASISTENCIA/NOTAS_ASISTENCIA/EXTRAS_ADMIN), troceado a 90 KB por el tope de 100 KB por valor. **`asistencia` 11 → 3 lecturas**, `roster` 9 → 1, `export` 7 → 2, `personal` 2 → 0. Fechas y horas se **normalizan a string antes de cachear** (sin eso, un turno de 07:00 volvía como "11:56" y los festivos se corrían un día). Invalidación automática en lo que escribe el script + **`?action=cache_reset`** y botón **"↻ Refrescar catálogos"** en el resumen para lo que se edita a mano en el Sheet. `CACHE_ON=false` lo apaga entero. **Calentador**: disparador por tiempo cada 30 min (`instalarCalentador`, una vez desde el editor) que relee los catálogos, para que la **primera carga del día** ya salga con el caché caliente y las ediciones a mano se vean solas en ≤30 min. El endpoint consolidado `?action=resumen_dia` **no aplica**: ya sale una sola petición por cambio de fecha. | ✅ Cerrado — desplegado y validado en campo (jul-2026) — D99 (Fase 1 + Fase 2 completa 1–5) |
| 4.11 | **Partición / archivado de `ASISTENCIA` por año — POSPUESTO a propósito (D99).** Con **<2.000 filas** no se justifica: el diagnóstico de D99 descartó el volumen como causa de la lentitud (el coste está en las 14 aperturas del Spreadsheet por petición, no en los datos). **Umbral para retomarlo: ~20–30 mil filas** en la hoja, o cuando `?action=export` supere los ~5 s de servidor medidos con el campo `_ms` de la Fase 2. Cuando llegue, hay que respetar los cruces que hoy leen todo el histórico (`proyectoDefecto` del export y el rango de `?action=ausencias`, D94). Hermano de 4.8 (archivado de BANDEJA/DATA/MAQUINARIA/VOLQUETAS), pero en el Sheet propio del módulo (D69). **Actualización jul-2026 (D102) — el umbral llega mucho antes de lo que sugería el <2.000 filas de D99:** con **300 personas reportando a diario** y ~26 días hábiles al mes son **5.200–7.800 filas/mes**, así que las **20–30 mil filas se alcanzan en 2,5–4 meses**, no en años (medido en banco: 300 personas × 60 días = **17.760 filas**; × 80 días = 23.680). **La lectura en dos pasos de D102 APLAZA este ítem, no lo elimina:** baja las celdas por petición de 305.884 a 26.739 con 2,4 meses de histórico (11,4×) y de 1,31 M a 87.693 con un año (15,0×), y la ganancia CRECE con el histórico porque las filas del día son constantes — pero **lo que queda sí sigue creciendo linealmente**: el escaneo de la columna `fecha` (1/17 de la hoja), `proyectoDefecto` del export (10/17) y los CC recientes de `roster` (9/17), todos cruces que miran TODO el histórico y no se pueden acotar por fecha sin cambiar el resultado. **Y lo que D102 NO tocó a propósito es lo que primero va a doler:** las rutas de escritura hacen `clearContents()` + reescritura completa, así que **cada envío de asistencia lee 17·N celdas y escribe otras 17·N** (~1,3 M + 1,3 M con un año de histórico). Cuando se retome, ese borrado quirúrgico de filas pesa más que el archivado. **Nuevo umbral sugerido para retomarlo:** cuando `_celdas` (campo nuevo de D102, visible en toda respuesta) pase de ~500.000 en `?action=export`, o cuando `_ms` de servidor supere los ~5 s. | ✅ **CERRADO por D107 (jul-2026): la escritura ya no reescribe la hoja.** `guardarAsistencia` y `guardarIndividual` borran solo el bloque que pisan (por tramos contiguos, 1 `deleteRows`) y anexan al final; el predicado se decide leyendo 3 y 5 de las 17 columnas. Medido: **10,9× menos celdas**, y el coste de una corrección deja de depender del histórico. El **archivado** propiamente dicho (mover años cerrados a otro Sheet) queda dentro de 4.8, ya sin urgencia |
| 4.12 | **Asistencias de UF3 / proyecto `3703` (D101).** ⚠️ **Lo único que falta es un dato en el Sheet vivo, no código:** la fila `proyecto_3703` = `3703| T2 - UF3 - R4513 PR 09+800 - PR 90+718` en la hoja CONFIG (ver 4.3). Sin ella el Parte de UF3 sale con "3703" pelado en la columna O y Navision no lo reconoce; la pantalla ya avisa en naranja al descargar. Rol nuevo `asistencia_plus_uf3` (usuario `residente_uf3`, `areas:['uf3']`) que reporta cualquier cuadrilla activa de UF3 —incluidos días anteriores—, revisa el resumen, completa faltantes, consulta ausencias, exporta el Parte de `3703` y gestiona su personal. UF3 entra como **área más** en el MISMO Sheet/Apps Script (D69/D72), no como módulo. `proyectoFromCC` ya era genérico; lo que se generalizó fue el FRONTEND (selector de UF, respaldo del CC del capataz, botones de export y string de proyecto por clave `CONFIG['proyecto_'+prefijo]`). `CONFIG.proyecto_3703` sembrado con el string real de la plantilla de UF3. | ⚠️ Desplegado y en uso, **pero falta la fila `proyecto_3703` en la hoja CONFIG viva** (ver abajo) — jul-2026, D101 |
| 4.13 | **Horas por persona — la vista inversa del resumen (D112).** Pantalla nueva `horas-persona.html` ("🕐 Horas por persona") + endpoint **`?action=persona&codigo=&cedula=&desde=&hasta=`** (solo lectura, tope 186 días, acotado al área del usuario POR EL BACKEND). Responde "¿cuántas horas me cargaron y por qué me pagaron esto?": una persona, un período, su detalle día a día y los totales por concepto. **Corte 11→10** (`CORTE_DIA=10`, el corte de extras y novedades del dueño) con botones de período actual/anterior/mes calendario y rango libre. Muestra ficha, tarjetas de totales nombradas como las columnas C·D·E·F·G·H del Parte, tabla día a día (avisos en naranja como el export), desgloses por CC y por motivo de ausencia, CSV y "copiar resumen" para WhatsApp. **Clave del ítem:** el clasificador de horas se EXTRAJO a `horas-nomina.js` y lo usan las DOS pantallas — el Parte de Navision y esta —, para que no puedan divergir; el backend manda el crudo y clasifica el cliente. Verificado con arneses en Node (`backend/pruebas/`, precedente D99/D102): el Parte antes/después del refactor es idéntico en **41.652 celdas A–R**, los totales de la pantalla son la suma de las filas del Parte, y el cerrojo de área rebota en el backend. Buscador de personas SIN endpoint nuevo (reusa `?action=personal`, con retirados y eventuales). | ✅ Hecho — **pendiente de REDESPLIEGUE** del Apps Script de asistencias (endpoint nuevo) y de validar en campo el supuesto del día 10 y un cotejo contra el Excel del Parte de un día real |
| 4.14 | **Cuadrilla propia para el usuario de asistencias (`angie`, D119) — NO implementar hasta que la pida.** Si algún día tiene gente que **no cuelga de ningún capataz** (el papel que `OPERADORES` cumple para `jeisson` y `DUVAN` para `duvan`, D105), es **una fila en la hoja CUADRILLAS y CERO código**: `cuadrilla=ANGIE · responsables=angie · area=<la que toque> · estado=` (vacío = activa). La rama de área de `cuadrillasDeUsuario` ya la devolvería —y la de `responsables` también la encontraría—, así que no hay nada que programar; la gente se le asigna moviéndola desde la gestión de personal del resumen. **En D119 el dueño NO la pidió**, así que se deja aquí a propósito en vez de sembrarla "por si acaso": una cuadrilla vacía de más ensucia el selector del formulario y los KPI del resumen. | Backlog — patrón D105, sin código |


## V3 — Largo plazo

| # | Ítem | Origen |
|---|---|---|
| **V3-01** | **Parte Digital de Maquinaria — 🔄 EN CURSO (D165, sep-2026).** Reemplaza al digitador del parte físico: `parte.html?eq=<codigo>` (público, QR en cabina, solo crea filas `pendiente`) → `PARTE_BANDEJA` → `revision-maquinaria.html` (login; pendientes con alertas, ✓/✕, edición en el sitio, «Aprobar todo lo sin alertas», equipos sin parte + «Agregar manual», y la Base con «Copiar para Excel» B→AR de `BASE MAQUINARIA`). Backend `backend/CodigoParte.gs` (`?mod=parte&op=equipo|reporte|bandeja|revisar|base`), 5 hojas `PARTE_*`, `setupParte()`, `tools/generar_qr.py`. **Queda por hacer (usuario):** correr `setupParte()`, importar los 4 CSV de `backend/seeds/parte/`, redesplegar, confirmar la URL base del QR e imprimir `qr/etiquetas.pdf`, **verificar el mapeo B→AR contra el Excel real** (§4 del prompt) pegando una semana de partes reales, dar de alta el usuario `parte_maquinaria` en `USUARIOS` si se quiere una persona dedicada, y depurar los operadores duplicados de `PARTE_OPERADORES` (no los funde el código). | Prompt V3-01 (`docs/PROMPT_V3-01_parte_digital_maquinaria.md`) |
| V3-02 | **Parte Digital — mejoras anotadas al construir V3-01, NO pedidas:** cola offline del formulario público (hoy exige señal; el operador reintenta con el mismo `id_registro` y no se duplica), lectura acotada de `PARTE_BANDEJA` por fecha cuando el histórico pese (hoy: columnas N×1 para el último final y filas del día por `readSheetPorFecha_`), consumos de diésel / valor hora / telemetría / aviso por WhatsApp, y `activo=NO` de un equipo desde la pantalla (hoy se edita la hoja). Solo si el dueño lo pide. | D165 |
| 3.2 | Chequeadora del Diviso digitalizada (hoy reporta por foto, otra persona) | Conversación fuentes |
| 3.3 | Materiales MSR con columnas propias en el resumen diario (hoy solo en DATA por ítem) | Análisis Excel |
| 3.6 | **Lectura en DOS PASOS de las hojas grandes (BANDEJA, DATA, MAQUINARIA) — el lever de verdad para cuando el histórico pese.** Hoy los endpoints por fecha leen la hoja ENTERA y filtran en memoria: `bandeja` 6 lecturas, `maquinaria_produccion` 4, `consolidado` 1 sobre DATA completa. D100 quitó las aperturas repetidas del Spreadsheet, pero el volumen leído no cambió y es lo que crece. **Idea:** leer primero SOLO la columna `fecha` (una llamada barata), quedarse con los índices de fila del día y traer únicamente esas filas. Medido en banco: `bandeja` pasaría de **324.120 celdas a 13.962 con un año de histórico (~23×)** y de 648.240 a 27.102 con dos años. **No se hizo en D100** porque no es un cambio mecánico: hay que verificar fila por fila que el filtrado da lo mismo, y conviene decidirlo con el `_ms` real de cada endpoint (que D100 ya expone). Hermano de 4.8 (archivado del histórico): esto evita tener que archivar tan pronto. **Actualización jul-2026 — el lever ya está PROBADO en producción, pero en el OTRO Sheet:** D102 lo implementó sobre `ASISTENCIA` (módulo de asistencias, Sheet y Apps Script propios), con helper único `leerFilasPorFecha_` (escaneo de la columna `fecha` + traída por bloques contiguos), cuatro umbrales medidos (`GAP_TOLERANCIA=5`, `MAX_BLOQUES=12`, `UMBRAL_COBERTURA=40 %`, `MIN_FILAS_PARA_DOS_PASOS=2.000`), fallback a la lectura completa y memo acotado SEPARADO del de la hoja completa. Resultado real: **305.884 → 26.739 celdas (11,4×)** en `?action=asistencia` con 2,4 meses de histórico y **15,0×** con un año, con 612 respuestas idénticas carácter a carácter. **Este ítem sigue ABIERTO para `Codigo.gs`** (BANDEJA/DATA/MAQUINARIA): es donde estaba la medición de `bandeja` 324.120 → 13.962, va en su propio prompt y no se toca desde el módulo de asistencias. Al portarlo, reusar el diseño de D102 tal cual — incluidas la advertencia del memo acotado y las guardas de coste cero antes del escaneo — y **NO aplicarlo a las rutas de escritura** por la misma razón que allá. (Nota: el texto de D100 remite esta idea al "backlog 4.12"; el ítem correcto es **este 3.6** — 4.12 acabó siendo las asistencias de UF3.) | ✅ **CERRADO por D107 (jul-2026)** — el lector en dos pasos portado a `Codigo.gs`, con variante por NOMBRE de columna y variante CRUDA por índice (para los lectores de DATA, que direccionan el layout A–T del maestro). Enganchado en `bandeja`, `estado`, `maquinaria_produccion`, `volquetas`, `consolidado` de un día y `debug`; `enviar_data` escanea 3 columnas y borra por tramos. Campo `_celdas` añadido también en obra. Medido en banco: **7,0×** con 2.400 filas × 8 columnas, y sube en las hojas reales (BANDEJA 28 col., MAQUINARIA 40) |
| 3.4 | Inoperativos estructurados (hoy texto libre) | D28 |
| 3.5 | **Sistema espejo para UF3** (mismo motor, con Sheet / centros de costo `3703` / personal / cuadrillas / pantallas propios). Solo si hay disponibilidad; fuera de alcance V2.  **Parcialmente cerrado por D101** en su parte de **asistencias** (rol `asistencia_plus_uf3`, área `uf3`, proyecto `3703`, backlog 4.12); sigue abierto todo el lado de obra (reporte, bandeja, DATA, maquinaria, panel). | D84 |

**Regla:** no inventar funcionalidades fuera de este backlog.
